{
  "name": "monopolio-ai-bank",
  "version": "1.0.0",
  "description": "Gerenciador de partidas de Monopólio com Eventos de IA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5",
    "@google/generative-ai": "^0.1.3",
    "dotenv": "^16.3.1"
  }
}
